export const YIC_ABOUT = `
YIC (Your Integrated Controller) is a small hub that routes natural-language
commands to connected devices over serial. It exposes a device registry with
logical names (e.g., "lights", "fan") mapped to ports, baudrates and payloads.
`;

// TODO: Expand on this